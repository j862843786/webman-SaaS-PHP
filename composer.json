{
  "name": "webman/saas-foundation",
  "description": "Webman SaaS foundation with tenant, multi-application and plugin extension support.",
  "type": "project",
  "require": {
    "php": ">=8.1",
    "workerman/webman-framework": "^1.6",
    "illuminate/database": "^10.0|^11.0",

  },
  "autoload": {
    "psr-4": {
      "app\\": "app/"
    }
  },
  "scripts": {
    "check": "find app config bin -name '*.php' -print0 | xargs -0 -n1 php -l && php -l start.php && php -l public/index.php"
  },
  "config": {
    "sort-packages": true
  },
  "license": "proprietary"
}
